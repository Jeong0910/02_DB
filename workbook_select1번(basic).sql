

--1번

SELECT DEPARTMENT_NAME "학과 명", CATEGORY "계열"
FROM TB_DEPARTMENT;


--2번

SELECT DEPARTMENT_NAME "학과 명", CAPACITY 정원
FROM TB_DEPARTMENT;

--3번

SELECT TB_DEPARTMENT, STUDENT_NAME, ABSENCE_YN, STUDENT_SSN
FROM TB_STUDENT
WHERE INSTR(TB_DEPARTMENT, '001') AND
INSTR(ABSENCE_YN,'Y') AND
INSTR(STUDENT_SSN,'2');


--4번
SELECT STUDENT_NO, STUDENT_NAME
FROM TB_STUDENT
WHERE STUDENT_NO IN('A513079', 'A513090', 'A513091', 'A513110', 'A513119');


--5번
SELECT DEPARTMENT_NAME, CATEGORY
FROM TB_DEPARTMENT
WHERE CAPACITY BETWEEN '20' AND '30';

--6번
SELECT PROFESSOR_NAME, DEPARTMENT_NO
FROM TB_PROFESSOR
WHERE DEPARTMENT_NO IS NULL;

--7번
SELECT DEPARTMENT_NO, STUDENT_NAME
FROM TB_STUDENT;
WHERE

--8번
SELECT PREATTENDING_CLASS_NO
FROM TB_CLASS
WHERE PREATTENDING_CLASS_NO IS NOT NULL;

--9번
SELECT DEPARTMENT_NO, DEPARTMENT_NAME, CATEGORY
FROM TB_DEPARTMENT


--10번
SELECT STUDENT_NO, STUDENT_NAME, STUDENT_SSN, INSTR(ABSENCE_YN,'Y'), STUDENT_ADDRESS
FROM TB_STUDENT
WHERE INSTR(STUDENT_ADDRESS, '전주시');



















